diff --git a/node_modules/mem0ai/dist/oss/index.mjs b/node_modules/mem0ai/dist/oss/index.mjs
index d93a6f2..8e62fff 100644
--- a/node_modules/mem0ai/dist/oss/index.mjs
+++ b/node_modules/mem0ai/dist/oss/index.mjs
@@ -2278,7 +2278,8 @@ var Qdrant = class {
         await this.createFilterIndexes(name);
       }
     } catch (error) {
-      if ((error == null ? void 0 : error.status) === 409 || (error == null ? void 0 : error.status) === 401 || (error == null ? void 0 : error.status) === 403) {
+      const legacyQdrantAlreadyExists = error?.status === 400 && String(error?.data?.status?.error ?? "").toLowerCase().includes("already exists");
+      if ((error == null ? void 0 : error.status) === 409 || (error == null ? void 0 : error.status) === 401 || (error == null ? void 0 : error.status) === 403 || legacyQdrantAlreadyExists) {
         if (name === this.collectionName) {
           try {
             const collectionInfo = await this.client.getCollection(name);
@@ -6929,7 +6930,7 @@ var AzureAISearch = class {
 };
 
 // src/oss/src/vector_stores/pgvector.ts
-import pkg from "pg";
+let pkg = {}; try { pkg = (await import("pg")).default; } catch (e) { console.warn("[mem0-patch] pg not installed (peer-skipped) — expected on boot per W6.2"); }
 var { Client, escapeIdentifier } = pkg;
 var SAFE_IDENTIFIER_RE = /^[a-zA-Z_][a-zA-Z0-9_]{0,127}$/;
 function validateIdentifier(name, label = "identifier") {
