# ─── Required ────────────────────────────────────────────────────────────
# LLM provider for mem0's extraction + embedding pipeline
OPENAI_API_KEY=

# User ID that scopes memories. Use any string; it's the namespace.
MEM0_USER_ID=default

# ─── Server tuning ───────────────────────────────────────────────────────
# Port the HTTP server binds to. Container-internal. Exposed via compose.
MEM0_MCP_PORT=6335

# ─── Optional — override defaults ────────────────────────────────────────
# Embedding model (OpenAI). text-embedding-3-small is cost-effective and good.
# MEM0_EMBEDDER_MODEL=text-embedding-3-small

# Extraction LLM (OpenAI). Smaller = cheaper, less nuance.
# MEM0_LLM_MODEL=gpt-4.1-nano

# Qdrant connection (internal to docker network — usually no need to change)
# QDRANT_HOST=qdrant
# QDRANT_PORT=6333
# QDRANT_COLLECTION=memories
